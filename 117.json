import 'dart:convert';

// 1. Convertir el texto plano (String) en un mapa de datos
Map<String, dynamic> datosExamen = jsonDecode(tuCadenaJson);

// 2. Para acceder al texto de traducción:
String textoIngles = datosExamen['seccion_traduccion']['texto_ingles'];

// 3. Para acceder a la lista de preguntas del multiple choice:
List<dynamic> preguntas = datosExamen['seccion_multiple_choice']['preguntas'];
